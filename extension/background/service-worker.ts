// Background service worker. Currently unused — the popup fetches data from the API
// and injects the engine directly. Kept as a manifest entry point for future
// background work (e.g. an agent command broker).
export {}
